[project]
name = "rag-from-scratch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "arize-phoenix[evals]>=12.7.1",
    "pytest>=8.4.2",
    "pyyaml>=6.0.3",
]
